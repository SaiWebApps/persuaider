import { ConversationReviewClient } from './ConversationReviewClient';

export const metadata = {
  title: 'Conversations - Persuaider Admin',
};

export default function ConversationsPage() {
  return <ConversationReviewClient />;
}